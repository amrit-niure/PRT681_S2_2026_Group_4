# Base URL of the WorkforceApi (ASP.NET Core). Used by Next.js on the server and by the /backend rewrite.
# When the app is started through the Aspire AppHost this is injected automatically.
# Copy this file to .env.local when running the API on its own: `dotnet run --project backend/WorkforceApi`.
API_URL=http://localhost:5280
